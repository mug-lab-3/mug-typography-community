name: Approve Work Submission

# Opens the pull request that lands a submission. Triggered by adding the
# `approved` label (or dispatched with an issue number); merging that pull
# request is what actually publishes the work.
on:
  issues:
    types: [labeled]
  workflow_dispatch:
    inputs:
      issue_number:
        description: Issue number to approve
        required: true

permissions:
  issues: write
  contents: write
  pull-requests: write

jobs:
  approve:
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.label.name == 'approved'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # ffprobe reads the attachments' dimensions, duration and format. It is
      # not present on the runner image by default, so it is installed here
      # rather than assumed.
      - name: Install ffprobe
        run: |
          if ! command -v ffprobe > /dev/null; then
            sudo apt-get update && sudo apt-get install -y ffmpeg
          fi
          ffprobe -version | head -1

      - name: Resolve issue number
        id: resolve
        run: echo "number=${{ github.event.inputs.issue_number || github.event.issue.number }}" >> "$GITHUB_OUTPUT"

      - name: Fetch issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh issue view "${{ steps.resolve.outputs.number }}" --json number,url,body,author > issue.json

      # Re-validated rather than trusting the earlier `work:valid` label:
      # the issue body may have been edited after that check ran, and this is
      # the step that decides what gets committed.
      - name: Validate and emit work files
        run: node scripts/validate-work.mjs issue.json --emit gallery > report.md 2>&1

      - name: Rebuild catalog
        run: node scripts/build-catalog.mjs

      - name: Create pull request
        id: pr
        uses: peter-evans/create-pull-request@v6
        with:
          branch: work/issue-${{ steps.resolve.outputs.number }}
          title: "Add gallery work from #${{ steps.resolve.outputs.number }}"
          body: |
            Adds the work submitted in #${{ steps.resolve.outputs.number }}.

            Validated against the media limits and the embedded script was
            extracted from the recording. Merging publishes it to the
            simulator's gallery.
          commit-message: "Add gallery work from #${{ steps.resolve.outputs.number }}"
          add-paths: |
            gallery
            catalog.json

      - name: Comment outcome
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -n "${{ steps.pr.outputs.pull-request-url }}" ]; then
            gh issue comment "${{ steps.resolve.outputs.number }}" \
              --body "Pull request opened: ${{ steps.pr.outputs.pull-request-url }}"
          else
            gh issue comment "${{ steps.resolve.outputs.number }}" --body-file report.md
          fi
